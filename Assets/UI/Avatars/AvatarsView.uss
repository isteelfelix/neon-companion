/* ============================================================
   AvatarsView — gallery + preview card
   ============================================================ */

.avatars-page {
    flex-grow: 1;
    flex-direction: column;
    padding: 16px 20px 20px;
    overflow: hidden;
}

/* ---- View-mode switcher ---- */

.viewmode-row {
    flex-direction: row;
    margin-bottom: 14px;
    background-color: var(--bg-2);
    border-radius: 10px;
    padding: 3px;
    align-self: flex-start;
    border-width: 1px;
    border-color: var(--line-2);
}

.viewmode-btn {
    height: 28px;
    padding: 0 18px;
    border-radius: 7px;
    border-width: 0;
    background-color: transparent;
    color: var(--text-3);
    -unity-font-style: normal;
    font-size: 12px;
    transition-property: background-color, color;
    transition-duration: 0.12s;
}

.viewmode-btn Label {
    font-size: 12px;
}

.viewmode-btn--active {
    background-color: var(--bg-0);
    color: var(--text-0);
    border-width: 1px;
    border-color: var(--line-2);
}

/* ---- Gallery container ---- */

.gallery-container {
    flex-grow: 1;
    min-height: 0;
    flex-direction: column;
}

/* ---- Empty gallery hint ---- */

.gallery-empty-hint {
    color: var(--text-3);
    font-size: 12px;
    margin: 24px 0 0 4px;
    -unity-font-definition: url("project://database/Assets/Fonts/GeistMono/GeistMono-Regular.ttf");
}

/* ---- Capability facts ---- */

.capability-card {
    background-color: var(--bg-2);
    border-width: 1px;
    border-color: var(--line-2);
    border-radius: 10px;
    padding: 10px;
}

.capability-card__title {
    color: var(--text-0);
    font-size: 12px;
    -unity-font-style: bold;
    margin-bottom: 6px;
}

.capability-card__fact,
.capability-card__evidence {
    color: var(--text-2);
    font-size: 11px;
    margin-bottom: 4px;
    white-space: normal;
}

.capability-card__evidence {
    color: var(--text-3);
    margin-top: 3px;
}

.capability-card__diagnostic {
    color: #e5a955;
    font-size: 11px;
    margin-top: 5px;
    white-space: normal;
}

/* ---- Avatar import dialog ---- */

.avatar-import-overlay {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    align-items: center;
    justify-content: center;
}

.avatar-import-scrim {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    background-color: rgba(4, 5, 10, 0.82);
}

.avatar-import-dialog {
    width: 720px;
    max-width: 92%;
    max-height: 92%;
    background-color: var(--bg-1);
    border-width: 1px;
    border-color: var(--line-1);
    border-radius: 16px;
    padding: 18px;
}

.avatar-import-head {
    flex-direction: row;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 14px;
}

.avatar-import-title {
    color: var(--text-0);
    font-size: 18px;
    -unity-font-style: bold;
}

.avatar-import-subtitle,
.avatar-import-help {
    color: var(--text-3);
    font-size: 11px;
    margin-top: 4px;
    white-space: normal;
}

.avatar-import-close {
    width: 30px;
    height: 30px;
    border-width: 0;
    background-color: transparent;
    color: var(--text-2);
    font-size: 20px;
}

.avatar-import-types {
    flex-direction: row;
    flex-wrap: wrap;
    margin-bottom: 8px;
}

.avatar-import-type {
    height: 32px;
    margin-right: 8px;
    margin-bottom: 6px;
    padding: 0 13px;
    background-color: var(--bg-2);
    border-width: 1px;
    border-color: var(--line-2);
    border-radius: 8px;
    color: var(--text-2);
}

.avatar-import-type--active {
    background-color: var(--accent-soft);
    border-color: var(--accent);
    color: var(--accent-text);
}

.avatar-import-source-row {
    flex-direction: row;
    align-items: center;
    margin-top: 12px;
    margin-bottom: 12px;
}

.avatar-import-source {
    flex-grow: 1;
    color: var(--text-2);
    font-size: 11px;
    background-color: var(--bg-2);
    border-width: 1px;
    border-color: var(--line-2);
    border-radius: 8px;
    padding: 8px 10px;
    margin-right: 8px;
    white-space: normal;
}

.avatar-import-preview-row {
    flex-direction: row;
    margin-top: 4px;
    margin-bottom: 14px;
}

.avatar-import-preview-box {
    width: 210px;
    height: 210px;
    flex-shrink: 0;
    background-color: var(--bg-0);
    border-width: 1px;
    border-color: var(--line-2);
    border-radius: 12px;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    margin-right: 12px;
}

.avatar-import-preview-image {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
}

.avatar-import-preview-placeholder {
    color: var(--text-3);
    font-size: 12px;
    white-space: normal;
    -unity-text-align: middle-center;
    padding: 12px;
}

.avatar-import-capabilities {
    flex-grow: 1;
}

.avatar-import-mapping {
    margin-bottom: 12px;
}

.avatar-import-mapping-grid {
    flex-direction: row;
    flex-wrap: wrap;
    margin-top: 8px;
}

.avatar-import-clip {
    width: 48%;
    margin-right: 2%;
    margin-bottom: 6px;
}

.avatar-import-actions {
    flex-direction: row;
    justify-content: flex-end;
}

.ff-phone .avatar-import-dialog {
    width: 94%;
    max-width: 94%;
    max-height: 94%;
}

.ff-phone .avatar-import-preview-row {
    flex-direction: column;
}

.ff-phone .avatar-import-preview-box {
    width: 100%;
    height: 180px;
    margin-right: 0;
    margin-bottom: 10px;
}

.ff-phone .avatar-import-clip {
    width: 100%;
    margin-right: 0;
}

/* ---- Animated tile badge ---- */

.avtile__anim-badge {
    position: absolute;
    top: 10px;
    left: 10px;
    background-color: rgba(124, 122, 237, 0.85);
    border-radius: 5px;
    padding: 2px 6px;
}

.avtile__anim-badge Label {
    font-size: 9px;
    color: white;
    -unity-font-definition: url("project://database/Assets/Fonts/GeistMono/GeistMono-Medium.ttf");
    letter-spacing: 0.5px;
}

/* ---- Filter row ---- */

.filterrow {
    min-height: 24px;
    flex-direction: row;
    margin-bottom: 10px;
}

.filterchip {
    height: 24px;
    flex-direction: row;
    align-items: center;
    padding: 0 12px;
    border-radius: 8px;
    background-color: var(--bg-2);
    border-width: 1px;
    border-color: var(--line-2);
    color: var(--text-2);
    margin-right: 6px;
    /* Button reset */
    -unity-font-style: normal;
}

.filterchip Label {
    font-size: 12px;
}

.filterchip--active {
    background-color: var(--accent-soft);
    border-color: var(--accent);
    color: var(--accent-text);
}

.filterchip__count {
    font-size: 10px;
    color: var(--text-3);
    background-color: rgba(255, 255, 255, 0.04);
    border-radius: 6px;
    padding: 1px 5px;
    margin-left: 6px;
    -unity-font-definition: url("project://database/Assets/Fonts/GeistMono/GeistMono-Regular.ttf");
}

/* ---- Split layout ---- */

.avatar-split {
    flex-grow: 1;
    flex-direction: row;
    min-height: 0;
}

/* ---- Gallery grid ---- */

.gallery {
    flex-grow: 1;
    flex-direction: row;
    flex-wrap: wrap;
    align-content: flex-start;
    margin-right: 22px;
}

/* ============== Avatar tile ============== */

.avtile {
    width: 23%;
    height: 168px;
    min-width: 130px;
    border-radius: 14px;
    border-width: 1px;
    border-color: var(--line-1);
    background-color: var(--bg-1);
    margin-right: 14px;
    margin-bottom: 14px;
    padding: 12px;
    justify-content: flex-end;
    overflow: hidden;
    position: relative;
    -unity-background-scale-mode: scale-and-crop;
    transition-property: border-color, scale;
    transition-duration: 0.15s;
}

.avtile:hover {
    scale: 1.02 1.02;
}

.avtile--selected {
    border-color: var(--accent);
    border-width: 2px;
}

/* ---- Tile color variants ---- */
.avtile--neon   { background-color: #4f45d8; }
.avtile--yorha-2b { background-color: #24242b; }
.avtile--aurora { background-color: #3faec0; }
.avtile--ember  { background-color: #c84c2c; }
.avtile--glass  { background-color: #2d3240; }
.avtile--flora  { background-color: #32835c; }
.avtile--mono   { background-color: #9aa2b3; }
.avtile--cobalt { background-color: #304bd0; }
.avtile--rose   { background-color: #bf507c; }

/* ---- Vignette overlay (hidden — USS has no gradient support) ---- */

.avtile__vignette {
    display: none;
}

/* ---- Tile name ---- */

.avtile__name {
    position: absolute;
    left: 10px;
    bottom: 9px;
    color: rgba(255, 255, 255, 0.95);
    font-size: 12px;
    -unity-font-style: bold;
    background-color: rgba(0, 0, 0, 0.38);
    border-radius: 6px;
    padding: 2px 7px;
}

/* ---- Selection badge ---- */

.avtile__badge {
    display: none;
    position: absolute;
    top: 10px;
    right: 10px;
    width: 24px;
    height: 24px;
    border-radius: 999px;
    background-color: var(--accent);
    align-items: center;
    justify-content: center;
}

.avtile--selected .avtile__badge {
    display: flex;
}

.avtile__badge .icon {
    width: 12px;
    height: 12px;
    -unity-background-image-tint-color: white;
}

/* ============================================================
   Узкое окно: кнопки шапки переносятся под текст, фильтры — в несколько
   рядов (иначе «Открыть папку/Загрузить» и чипы фильтров уезжали за край).
   ============================================================ */
.app--compact .avatars-page {
    padding: 12px 14px 14px;
}

.app--compact .page__head {
    flex-wrap: wrap;
}

.app--compact .page__actions {
    margin-top: 10px;
}

.app--compact .filterrow {
    flex-wrap: wrap;
    height: auto;
}

.app--compact .filterchip {
    margin-bottom: 6px;
}

/* ---- Upload tile ---- */

.avtile--upload {
    background-color: var(--bg-1);
    padding: 6px;
}

.avtile__upload {
    flex-grow: 1;
    border-width: 1px;
    border-color: var(--line-2);
    border-radius: 10px;
    align-items: center;
    justify-content: center;
    color: var(--text-3);
}

.avtile__upload .icon {
    width: 22px;
    height: 22px;
    margin-bottom: 8px;
}

.avtile__upload Label {
    font-size: 11px;
    -unity-font-definition: url("project://database/Assets/Fonts/GeistMono/GeistMono-Regular.ttf");
}

/* ============== Preview card ============== */

.previewcard {
    width: 320px;
    min-width: 304px;
    flex-shrink: 0;
    background-color: var(--bg-1);
    border-width: 1px;
    border-color: var(--line-1);
    border-radius: 14px;
    padding: 14px 12px 14px 14px;
    flex-direction: column;
    min-height: 0;
}

/* Phone: the preview card is fixed 320px / no-shrink on desktop — make it fluid
   so it never forces a horizontal scroll on a narrow screen. */
.ff-phone .avatars-page {
    padding-left: 14px;
    padding-right: 14px;
}

.ff-phone .previewcard {
    width: 100%;
    min-width: 0;
    max-width: 100%;
    flex-shrink: 1;
}

.ff-phone .gallery {
    flex-wrap: wrap;
}

.preview-scroll {
    flex-grow: 1;
    min-height: 0;
}

.preview-scroll > .unity-scroll-view__content-container {
    padding-right: 4px;
}

.preview-hero {
    height: 184px;
    border-radius: 10px;
    background-color: #4f45d8;
    -unity-background-scale-mode: scale-and-crop;
    margin-bottom: 6px;
    justify-content: flex-end;
    padding: 14px;
}

.preview-hero__title {
    color: var(--text-0);
    font-size: 18px;
    -unity-font-style: bold;
}

.preview-hero__tag {
    color: var(--text-2);
    font-size: 11px;
    -unity-font-definition: url("project://database/Assets/Fonts/GeistMono/GeistMono-Regular.ttf");
}

.preview-emoji-overlay {
    position: absolute;
    top: 10px;
    right: 10px;
    font-size: 28px;
}

.preview-persona-label {
    margin-top: 8px;
}

.persona-state {
    margin-top: 2px;
    margin-bottom: 2px;
}

.persona-state__badge {
    align-self: flex-start;
    font-size: 10px;
    color: var(--accent-text);
    background-color: var(--accent-soft);
    border-width: 1px;
    border-color: var(--accent);
    border-radius: 6px;
    padding: 2px 8px;
    -unity-font-definition: url("project://database/Assets/Fonts/GeistMono/GeistMono-Medium.ttf");
}

.persona-state__help {
    margin-top: 6px;
    color: var(--text-3);
    font-size: 11px;
}

.persona-box {
    background-color: var(--bg-2);
    border-width: 1px;
    border-color: var(--line-2);
    border-radius: 10px;
    padding: 12px;
    color: var(--text-2);
    font-size: 11px;
    white-space: normal;
    -unity-font-definition: url("project://database/Assets/Fonts/GeistMono/GeistMono-Regular.ttf");
}

.preview-actions {
    flex-direction: row;
    flex-wrap: wrap;
    margin-top: 12px;
}

.preview-actions > Button,
.preview-actions__btn {
    width: 48.5%;
    margin-right: 8px;
    margin-bottom: 8px;
}

.preview-actions > Button.is-last,
.preview-actions__btn.is-last {
    margin-right: 0;
}

.preview-actions--compact > Button {
    width: 48.5%;
    margin-bottom: 0;
}

.persona-editor {
    flex-direction: column;
    margin-top: 10px;
    background-color: var(--bg-2);
    border-width: 1px;
    border-color: var(--line-2);
    border-radius: 10px;
    padding: 10px;
}

.persona-edit-field {
    height: 96px;
    margin-bottom: 10px;
    background-color: var(--bg-2);
    border-width: 1px;
    border-color: var(--line-2);
    border-radius: 10px;
}

.persona-edit-field > #unity-text-input {
    background-color: transparent;
    border-width: 0;
    padding: 12px;
    color: var(--text-2);
    font-size: 11px;
    white-space: normal;
    -unity-font-definition: url("project://database/Assets/Fonts/GeistMono/GeistMono-Regular.ttf");
}

.customize {
    margin-top: 12px;
    color: var(--text-2);
    padding-top: 4px;
}

.customize > .unity-foldout__content {
    margin-top: 8px;
    padding-top: 2px;
}

.customize__group {
    margin-top: 10px;
    background-color: var(--bg-2);
    border-width: 1px;
    border-color: var(--line-2);
    border-radius: 10px;
    padding: 10px;
}

.customize__emoji-grid {
    flex-direction: row;
    flex-wrap: wrap;
    margin-top: 4px;
}

.customize__emoji-btn {
    width: 34px;
    height: 30px;
    margin-right: 6px;
    margin-bottom: 6px;
    border-radius: 8px;
    border-width: 1px;
    border-color: var(--line-2);
    background-color: var(--bg-2);
}

.customize__emoji-btn.is-active {
    border-color: var(--accent);
    background-color: var(--accent-soft);
}
.companion-window-card {
    flex-direction: row;
    align-items: center;
    align-self: flex-start;
    max-width: 440px;
    padding: 8px 10px;
    margin-bottom: 12px;
    border-width: 1px;
    border-color: var(--line-2);
    border-radius: var(--r-lg);
    background-color: var(--bg-1);
}

.companion-window-card__copy {
    flex-grow: 1;
    margin-right: 10px;
}

.companion-window-card__title {
    color: var(--text-0);
    font-size: 14px;
    -unity-font-style: bold;
}

.companion-window-card__status {
    color: var(--text-3);
    font-size: 11px;
    margin-top: 2px;
}

.companion-settings-overlay {
    position: absolute;
}

.companion-settings-scrim {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
}

.companion-settings-modal {
    width: 520px;
    max-width: 94%;
}

.companion-settings-modal__head {
    flex-direction: row;
    align-items: flex-start;
    margin-bottom: 12px;
}

.companion-settings-modal__copy,
.companion-settings-modal__scale,
.companion-settings-modal__scale > Slider {
    flex-grow: 1;
}

.companion-settings-modal__title {
    color: var(--text-0);
    font-size: 16px;
    -unity-font-style: bold;
}

.companion-settings-modal__subtitle,
.companion-settings-modal__hint {
    color: var(--text-3);
    font-size: 11px;
    margin-top: 3px;
    white-space: normal;
}

.companion-settings-modal__close {
    min-width: 32px;
    margin-left: 10px;
}

.companion-settings-modal__options {
    margin-bottom: 8px;
}

.companion-settings-modal__options > * {
    margin-bottom: 8px;
}

.companion-settings-modal__actions {
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: flex-end;
    margin-top: 10px;
}

.companion-settings-modal__actions > .btn {
    margin-left: 6px;
    margin-bottom: 6px;
}
